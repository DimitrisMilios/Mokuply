import 'package:flutter/material.dart';

/// Centralized color design tokens for Mocuply.
class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0F172A);
  static const Color surface = Color(0xFF1E293B);
  static const Color surfaceBorder = Color(0xFF334155);
  static const Color surfaceSelected = Color(0xFF1E1B4B);
  static const Color canvasViewport = Color(0xFF0B0F19);

  // Primary
  static const Color primary = Color(0xFF6366F1);
  static const Color primaryDark = Color(0xFF4F46E5);
  static const Color primaryLight = Color(0xFFA5B4FC);

  // Secondary
  static const Color secondary = Color(0xFFA855F7);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);

  // Badges
  static const Color badgeBg = Color(0xFF312E81);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFE2E8F0);
  static const Color textMuted = Color(0xFF94A3B8);

  // Device Frames
  static const Color frameIphone = Color(0xFF334155);
  static const Color frameSamsung = Color(0xFF475569);
}
